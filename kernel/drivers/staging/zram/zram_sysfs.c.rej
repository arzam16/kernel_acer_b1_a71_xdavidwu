--- drivers/staging/zram/zram_sysfs.c
+++ drivers/staging/zram/zram_sysfs.c
@@ -123,10 +118,6 @@ static ssize_t reset_store(struct device *dev,
 up_write(&zram->init_lock);
 
 return len;
-
-out:
-bdput(bdev);
-return ret;
 }
 
 static ssize_t num_reads_show(struct device *dev,
